import sqlite3\nimport json\n\ndef export_members(db_file, output_file):\n    conn = sqlite3.connect(db_file)\n    cursor = conn.cursor()\n    \n    cursor.execute('SELECT section, num, nom, inscription_num, inscription_date, bp, tel1, tel2, email, adresse, ville FROM members')\n    rows = cursor.fetchall()\n\n    members_by_section = {}\n    for row in rows:\n        section = row[0]\n        if section not in members_by_section:\n            members_by_section[section] = []\n        member_info = {\n            'num': row[1],\n            'nom': row[2],\n            'inscription_num': row[3],\n            'inscription_date': row[4],\n            'bp': row[5],\n            'tel1': row[6],\n            'tel2': row[7],\n            'email': row[8],\n            'adresse': row[9],\n            'ville': row[10]\n        }\n        members_by_section[section].append(member_info)\n\n    with open(output_file, 'w') as f:\n        json.dump(members_by_section, f, ensure_ascii=False, indent=4)\n\nif __name__ == '__main__':\n    export_members('onecca.db', 'members_export.json')\n